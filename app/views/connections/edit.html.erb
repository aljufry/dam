<% @page_title = "Edit Connection" %>

<div class="pageTitle">
  <%=  "Edit Connection" %>
</div>

<div class="form">
  <%= form_for(:connection, :url => {:action => 'update', :id => @connection.id}) do |f| %>
  <% if @connection.errors.any? %>
  
  <div class="error">
    <ul>
      <% @connection.errors.full_messages.each do |message| %>
      <li>
        <%= message %>
      </li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <%= render(:partial => "form", :locals => {:f => f}) %>
  <% end %>
</div>
