<% @page_title = t(:subnet) %>


  <div style="text-align: center" ><font size="4" color="#191970">
    <%= @subnet.name %></font>
  </div>
  <table class="table table-hover">
    <tr>
      <td class="col-md-2" style="color: orange"><%= t :name %>:</td>
      <td><%= @subnet.name %></td>
    </tr>
    <tr>
      <td class="title" style="color: orange"><%= t :Location %>:</td>
      <td><%= @subnet.cidr %></td>
    </tr>
    <tr>
      <td class="title" style="color: orange"><%= t :description %>:</td>
      <td><%= @subnet.description %></td>
    </tr>
    <tr>
      <td class="col-md-2" style="color: orange"><%="Created at" %>:</td>
      <td><%= @subnet.created_at.to_s(:short) %><font color="red"><%="   by  " %></font><%= @subnet.author %></td>
    </tr>
    <tr>
      <td class=col-md-2" style="color: orange"><%= "Updated at"%>:</td>
      <%if @subnet.update_author == nil%>
          <td>----</td>
          <%else%>
      <td   ><%= @subnet.updated_at.to_s(:long) %><font color="red"><%="   by  " %></font><%= @subnet.update_author %></td>
          <%end%>
    </tr>


    <%if  session[:user_id]!=nil && User.find(session[:user_id].to_i).usertype != "Visitor" %>
    <tr>
      <td class="buttons" colspan="2">
        <%= link_to(t("list"), {:action => 'list'}, :class => "btn btn-info") %>
        <%= link_to(t("edit"), {:action => 'edit', :id => @subnet.id}, :class => "btn btn-warning") %>
      </td>
    </tr>
    <%end%>
  </table>
  <%if  session[:user_id]!=nil && User.find(session[:user_id].to_i).usertype == "Visitor" %>
      <div class="Third_links">
        <br>
        <%= link_to("Back",{:controller => 'subnets', :action => 'view_user'},:class=>"btn btn-info")%>
      </div>
  <%end%>

